- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bashrc_local_after:
    ~/.shell_local_after:
    ~/.shell_local_before:

- shell:
  - ./macos
