- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bashrc_local_after:
    ~/.gitconfig_local:
    ~/.shell_local_after:
    ~/.shell_local_before:

- shell:
  - ./macos
