# Homebrew bash completion set-up.
[ -f /usr/local/etc/bash_completion ] && . /usr/local/etc/bash_completion

#iterm2 shell integration.
test -e "${HOME}/.iterm2_shell_integration.bash" && source "${HOME}/.iterm2_shell_integration.bash"